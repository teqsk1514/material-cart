<div>
    <div class="row">
        <div class="col-md-8 offset-2">
            <h1>User Profile</h1>
            <hr>
            <h2 style="padding:30px">My Orders</h2>
            <a href="/"><button type="button" class="btn btn-raised btn-primary">Buy More Items</button></a>
            <hr>
            {{# each orders}}
            <div class="card" style="margin-bottom:30px">
                <div class="card-body">
                    <ul class="list-group">
                        {{# each this.items}}
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            {{this.item.title}} | {{this.qty}} Units
                            <span class="badge badge-primary badge-pill">{{this.price}}</span>
                        </li>
                        {{/each}}
                    </ul>
                </div>
                <div class="card-footer text-muted" style="float:right">
                    <strong>Total Price: $ {{this.cart.totalPrice}}</strong>
                </div>
            </div>
            {{/each}}

        </div>
    </div>
</div>